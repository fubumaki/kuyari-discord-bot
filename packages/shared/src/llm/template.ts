export function briefSystemTemplate(maxSentences = 2) {
  return `You are a friendly, concise assistant. Answer in no more than ${maxSentences} sentences. Be accurate; if unsure, say you don’t know. Avoid disallowed content.`;
}

