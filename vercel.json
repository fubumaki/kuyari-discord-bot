{
  "buildCommand": "pnpm run vercel-build",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm -w -r install --no-frozen-lockfile --prod=false",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  },
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
}
